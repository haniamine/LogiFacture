<%= form_with(model: produit, local: true) do |form| %>
  <% if produit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(produit.errors.count, "error") %> prohibited this produit from being saved:</h2>

      <ul>
        <% produit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :libelle %>
    <%= form.text_field :libelle %>
  </div>

  <div class="field">
    <%= form.label :prixHT %>
    <%= form.text_field :prixHT %>
  </div>

  <div class="field">
    <%= form.label :tauxTVA %>
    <%= form.text_field :tauxTVA %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
